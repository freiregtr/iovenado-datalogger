# IOVENADO DataLogger - Dependencies

# Qt framework for Python
PySide6>=6.5.0

# Serial communication
pyserial>=3.5

# Bluetooth communication (for remote control)
# Note: pybluez has issues with setuptools>=58
# Install manually with: pip install git+https://github.com/pybluez/pybluez.git#egg=pybluez
# Or on Raspberry Pi: sudo apt-get install python3-bluez
pybluez>=0.23; platform_system == "Linux"
