# IOVENADO DataLogger - Dependencies

# Qt framework for Python
PySide6>=6.5.0

# Serial communication
pyserial>=3.5

# Bluetooth communication (for remote control)
# NOTE: pybluez is NOT installed via pip due to setuptools compatibility issues
# On Raspberry Pi, install system package: sudo apt-get install python3-bluez
# Then create venv with: python3 -m venv --system-site-packages venv
